resources:
  schemas:
    price_transparency_schema:
      name: ${var.schema}
      grants:
        - principal: users
          privileges:
            - SELECT
            - READ_VOLUME
            - EXECUTE
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.catalog}